def count_in_list(lst, elem) -> int:
	"Count the element occurences in the list"

	return lst.count(elem)

def index_in_list(lst, elem) -> int:
	"Returns the element's index in the list"

	return lst.index(elem)